# World Cup 2026 Static Site

This repository contains a static World Cup 2026 landing page.

The entry point is `index.html`, and the frame images live in `frames60/`.

## GitHub Pages

This repo includes a GitHub Actions workflow at `.github/workflows/pages.yml`.
After the files are pushed to the `main` branch, the workflow publishes the site
to GitHub Pages automatically.
